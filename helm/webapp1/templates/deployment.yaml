apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "webapp1.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: webapp1
  template:
    metadata:
      labels:
        app: webapp1
    spec:
      serviceAccountName: {{ include "webapp1.fullname" . }}
      containers:
        - name: webapp1
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: WEBAPP2_BASE_URL
              value: {{ .Values.env.WEBAPP2_BASE_URL | quote }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
          livenessProbe:
            httpGet:
              path: /health
              port: http
